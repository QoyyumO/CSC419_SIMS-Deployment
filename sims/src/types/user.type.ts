/**
 * User Types
 * 
 * Type definitions for user-related types.
 * 
 * Note: User structure matches the database schema:
 * {
 *   email: string;
 *   hashedPassword: string;
 *   profile: { firstName: string; lastName: string };
 *   roles: UserRole[];
 * }
 */

